# Frontend Dashboard (React + Vite)

Quickstart:

```bash
cd frontend
npm install
npm run dev
```

- Dev server: http://localhost:5173
- Backend (FastAPI) should run on http://localhost:8000

Environment:
- `VITE_API_BASE` (optional) — base URL for backend endpoints.

Build for production:

```bash
npm run build
# copy dist/ to backend for FastAPI to serve (dashboard/dist)
```